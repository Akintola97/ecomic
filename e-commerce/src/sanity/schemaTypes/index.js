import { bannerContent } from "../schemas/bannerContent";
import { featuredProducts } from "../schemas/featuredProducts";
import { storeProducts } from "../schemas/storeProducts";


export const schema = {
  types: [bannerContent, featuredProducts, storeProducts],
}
